const Config = () => {
  return (
    <p>
      Ajustes de configuración----- Aqui poner un botón para un modo oscuro,
      poner para poder cambiar el nickname y la contraseña, ademas, cuando se
      implemente el poder compartir notas, añadir una foto de usuario y que se
      pueda cambiar WIP
    </p>
  );
};

export default Config;
